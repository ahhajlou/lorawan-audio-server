def build_ack(packet: Packet) -> bytes:
    """Pure function. Packet → raw bytes for ACK downlink."""

def build_downlink(packet: Packet) -> bytes:
    """Pure function. Packet → raw bytes for data downlink."""
