services:
  lorawan-audio-server:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
     - .env
    volumes:
      - sqlite-data:/app/data

volumes:
  sqlite-data:


